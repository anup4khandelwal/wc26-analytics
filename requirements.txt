soccerdata>=1.8.0
pymupdf>=1.24.0
# ScraperFC is optional (Sofascore fallback) — its GUI transitive deps fail
# to build on headless machines. Install manually if you want the fallback:
#   pip install ScraperFC
mplsoccer>=1.3.0
matplotlib>=3.8.0
pandas>=2.2.0
pyarrow>=15.0.0
requests>=2.31.0
numpy>=1.26.0
scipy>=1.12.0
Pillow>=10.0.0
